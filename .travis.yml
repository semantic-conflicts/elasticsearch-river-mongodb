
sudo: required
language: java

jdk:
  - openjdk8

script:
  - mvn clean compile assembly:single

before_deploy:
    - mkdir MiningBuild
    - find . -name '*.jar' -exec cp {} ./MiningBuild \;
    - cd /home/travis/build/semantic-conflicts/elasticsearch-river-mongodb/MiningBuild
    - tar -zcvf result.tar.gz *
deploy:
  provider: releases
  api_key: $GITHUB_TOKEN
  file: result.tar.gz
  name: fetchjar-3d3667e5f36ef35f02c49853551499e7e2ab5e19
  file_glob: true
  overwrite: true
  skip_cleanup: true
  on:
    all_branches: true 
            