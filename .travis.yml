
sudo: required
language: java

jdk:
  - openjdk8

script:
  - mvn clean compile assembly:single

before_deploy:
    - mkdir MiningBuild
    - find . -name '*.jar' -exec cp {} ./MiningBuild \;
    - cd /home/travis/build/semantic-conflicts/elasticsearch-river-mongodb/MiningBuild
    - tar -zcvf result.tar.gz *
deploy:
  provider: releases
  api_key: $GITHUB_TOKEN
  file: result.tar.gz
  name: fetchjar-3d4f99516ba3177f7d88a1f600923138a8b77cc8
  file_glob: true
  overwrite: true
  skip_cleanup: true
  on:
    all_branches: true 
            